// TODO: stub. Reportes de spam no portados en v1 (tabla lead_spam_reports inexistente).

export interface SpamReportDialogProps {
  open: boolean;
  onClose: () => void;
  leadId?: number;
}

export default function SpamReportDialog(_props: SpamReportDialogProps) {
  return null;
}
