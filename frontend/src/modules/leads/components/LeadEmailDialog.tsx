// TODO: stub. Envio manual de email a un lead no portado en v1
// (depende de lead_emails y de la tabla brevo templates).

export interface LeadEmailDialogProps {
  open: boolean;
  onClose: () => void;
  leadId?: number;
}

export default function LeadEmailDialog(_props: LeadEmailDialogProps) {
  return null;
}
