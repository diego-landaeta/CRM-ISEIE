// TODO: stub. Listado de emails enviados al lead no portado en v1
// (depende de tabla lead_emails).

export interface LeadEmailsCardProps {
  leadId?: number;
}

export default function LeadEmailsCard(_props: LeadEmailsCardProps) {
  return null;
}
